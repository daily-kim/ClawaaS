/**
 * Purpose: Base Next.js configuration for the ClawaaS frontend.
 * TODO: Add environment-specific API rewrites and production hardening once deployment targets are defined.
 */

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
